<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Notes.Models"
             x:Class="Notes.Views.AboutPage"
             Title="About">
    <ContentPage.BindingContext>
        <models:About />
    </ContentPage.BindingContext>
    <ScrollView>
        <VerticalStackLayout Spacing="10" Margin="10">
            <Label FontSize="Subtitle" FontAttributes="Bold" Text="Mathias Vera" HorizontalOptions="Center" />
            <Label FontSize="Body" Text="Soy estudiante de ingeniería en software cursando el 5to semestre, me gustan los musicales y leer" HorizontalOptions="Center" Margin="0,10" />

            <Image Source="bobpreocupado.png"
                   Aspect="AspectFill"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />

            <Button Text="Learn more..." Clicked="LearnMore_Clicked" />
        </VerticalStackLayout>
    </ScrollView>
    

</ContentPage>